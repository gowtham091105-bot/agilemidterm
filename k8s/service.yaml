apiVersion: v1
kind: Service
metadata:
  name: user-registration-svc
spec:
  type: NodePort
  selector:
    app: user-registration
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080
